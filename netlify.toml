[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Required for React SPA — all routes fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
